import { NodeToolbarDirective } from './node-toolbar.directive';

describe('NodeToolbarDirective', () => {
    it('should be defined', () => {
        expect(NodeToolbarDirective).toBeDefined();
    });
});
